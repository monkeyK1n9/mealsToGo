// HERE WE COMBINE OUR THEME

import {colors} from './colors'
import {space, lineWeight} from './spacing'
import {sizes} from './size'
import {fonts, fontWeights, fontSizes} from './fonts'

export const theme = {
    colors,
    space,
    lineWeight,
    sizes,
    fonts,
    fontWeights,
    fontSizes
}